Event bus for flows. Allows flows to send events as an action and to
trigger on events.

What are events?

Events are messages to indicate that something has happened. Events have a (user-definable) name, and optionally a value.
